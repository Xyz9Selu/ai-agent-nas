# PostgreSQL connection for document_rows persistence
POSTGRES_HOST=localhost
POSTGRES_PORT=25432
POSTGRES_DB=aiagent
POSTGRES_USER=aiagentuser
POSTGRES_PASSWORD=<your_password>
TABLE_NAME=document_rows

# Optional: SAP_PARSER_DB_BATCH_SIZE=500 (default: 500)
